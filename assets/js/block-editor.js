/**
 * Scripts pour l’éditeur de blocs WordPress (Gutenberg), chargé uniquement dans l’admin
 * lorsque l’écran utilise l’éditeur de blocs.
 *
 * Ajoutez ici des personnalisations (filtres, désenregistrement de blocs, etc.).
 * Les dépendances déclarées en PHP (wp-blocks, wp-dom) permettent d’utiliser l’API
 * WordPress si vous en avez besoin.
 */
(function () {
	// Fichier volontairement minimal : évite une erreur 404 tant qu’aucune logique n’est ajoutée.
})();
